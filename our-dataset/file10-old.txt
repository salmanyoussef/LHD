limit = 20
primes = []

for n in range(2, limit + 1):
    is_prime = True
    for d in range(2, int(n**0.5) + 1):
        if n % d == 0:
            is_prime = False
            break
    if is_prime:
        primes.append(n)

print(primes)

