def count_words(sentence):
    result = {}
    for token in sentence.split():
        if token in result:
            result[token] += 1
        else:
            result[token] = 1
    return result

text = "hello world hello program"
counts = count_words(text)
print(counts)

